spring:
  profiles:
    active: test
  datasource:
    url: jdbc:postgresql://localhost:5433/sicredi_avaliacao_test
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        lob.non-contextual-creation: true
    properties:
      hibernate:
        show_sql: true

user-info:
  url: 'https://user-info.herokuapp.com'
